<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.study.robin.managementapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".ClassActivity" />
        <activity android:name=".StudentPagerActivity" />
        <activity android:name=".LessonActivity" />
        <activity android:name=".ClassPagerActivity" />
        <activity android:name=".StudentChangeActivity" />
        <activity android:name=".GradeActivity" />
        <activity android:name=".ClassChangeActivity" />
        <activity android:name=".LessonChangeActivity" />
        <activity android:name=".LessonPagerActivity" />
        <activity android:name=".GradeChangeActivity" />
        <activity android:name=".AccountActivity"></activity>
    </application>

</manifest>